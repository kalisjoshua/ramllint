#%RAML 0.8

title: test
baseUri: http://api.quickenloans.com
version: 1

/resource:
  description: info
  get:
    description: collection GET
    responses:
      200:
        description: success
        body:
          application/json:
            example: |
              {}
  post:
    description: different
    body:
      application/json:
        example: |
          {
            "name": "Bobby Drop Tables"
          }
    responses:
      202:
        description: accepted
        body:
          application/json:
            example: |
              {
                "name": "new"
              }
  /{id}:
    description: more info
    get:
      description: entity GET
      responses:
        200:
          description: success
          body:
            application/json:
              example: |
                {}
        400:
          description: fail
          body:
            application/json:
              example: |
                {}
    /sub_resource:
      description: Not methods added intentionally for test coverage.
      get:
        description: more info
        responses:
          200:
            description: success
            body:
              application/json:
                example: |
                  {}
          500:
            description: fail
            body:
              application/json:
                example: |
                  {}
